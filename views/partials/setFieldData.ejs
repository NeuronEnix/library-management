<% if( typeof fieldData !== "undefined" ) { %>
    <script type="text/javascript">
        var fieldData = <%-JSON.stringify(fieldData)%>;
        for( const key in fieldData ) {
            document.getElementById( key ).value = fieldData[ key ];
            
        }
    </script>        
        
    <% } %> 